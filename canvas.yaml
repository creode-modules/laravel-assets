preset: package

namespace: Creode\LaravelAssets
user-auth-model: App\Models\User

paths:
  src: src
  resource: resources

factory:
  path: database/factories

migration:
  path: database/migrations
  prefix: ''

console:
  namespace: Creode\LaravelAssets\Console

model:
  namespace: Creode\LaravelAssets\Models

provider:
  namespace: Creode\LaravelAssets\Providers

testing:
  namespace: Creode\LaravelAssets\Tests
